{
  "name": "agntux-mcpjam-inspector",
  "private": true,
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "heroku-postbuild": "cd sdk && npm install --legacy-peer-deps --include=dev && npm run build && cd ../mcpjam-inspector && npm install --legacy-peer-deps --include=dev && npm run build && npm prune --legacy-peer-deps --omit=dev && rm -rf ../sdk/node_modules",
    "start": "cd mcpjam-inspector && node dist/server/index.js"
  }
}
